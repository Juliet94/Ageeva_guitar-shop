export const getGuitars = (state) => state.guitars;

export const getCart = (state) => state.cart;

export const getTotalPrice = (state) => state.totalPrice;

export const getDiscount = (state) => state.discount;

export const getFilterPriceFrom = (state) => state.filter.price.from;
export const getFilterPriceTo = (state) => state.filter.price.to;
export const getFilterType = (state) => state.filter.types;
export const getFilterStrings = (state) => state.filter.strings;
